package main

func findMaxSlidingWindow(nums []int, size int) []int {
	var ret []int
	var window []int
	L := len(nums)
	if L == 0 || size == 0 {
		return ret
	}

	// the 1st window elements
	for i := 0; i < size; i++ {

	}
	return ret
}
