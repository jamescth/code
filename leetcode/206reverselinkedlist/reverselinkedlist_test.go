package main

/*
func TestDeleteDuplicates(t *testing.T) {
	tests := []struct {
		n   int
		x   []int
		ret []int
	}{
		{1, []int{1, 1, 2}, []int{1, 2}},
		{2, []int{1, 1, 2, 3, 3}, []int{1, 2, 3}},
	}

	for _, tc := range tests {
		var l1, l2 *util.ListNode
		for _, n := range tc.x {
			util.Insert(&l1, n)
		}
		for _, n := range tc.ret {
			util.Insert(&l2, n)
		}

		if ret := deleteDuplicates(l1); !util.CompareListNodes(ret, l2) {
			util.Show(ret)
			util.Show(l2)
			t.Errorf("%d failed %v expect %v got %v\n", tc.n, tc.x, l2, ret)
		}
	}
}
*/
